<h1> <%= @playlist.name %> </h1>

<ul>
  <% @songs.each do |song| %>
    <li><%= link_to song.title, song_path(song) %></li>
  <% end %>
</ul>
<%= link_to "Edit Playlist", edit_playlist_path(@playlist) %>
